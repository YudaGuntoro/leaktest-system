import FormManualPage from "@/production/FormManualPage";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Form Manual | PT. Yanmar Diesel Indonesia" };

export default function FormManualRoute() {
  return <FormManualPage />;
}
